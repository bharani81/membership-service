# name: CI Pipeline

# on:
#   push:
#     branches: [ main, develop, 'feature/**' ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   build-and-test:
#     name: Build → Test → Analyse → Package
#     runs-on: ubuntu-latest

#     services:
#       postgres:
#         image: postgres:16-alpine
#         env:
#           POSTGRES_DB: membership_db
#           POSTGRES_USER: membership_user
#           POSTGRES_PASSWORD: membership_pass
#         ports:
#           - 5432:5432
#         options: >-
#           --health-cmd pg_isready
#           --health-interval 10s
#           --health-`timeout 5s
#           --health-retries 5

#     steps:
#       - name: Checkout source
#         uses: actions/checkout@v4

#       - name: Set up Java 21
#         uses: actions/setup-java@v4
#         with:
#           java-version: '21'
#           distribution: 'temurin'
#           cache: maven

#       - name: Build (compile only)
#         run: mvn compile -B

#       - name: Run unit & integration tests
#         run: mvn test -B
#         env:
#           SPRING_DATASOURCE_URL: jdbc:postgresql://localhost:5432/membership_db
#           SPRING_DATASOURCE_USERNAME: membership_user
#           SPRING_DATASOURCE_PASSWORD: membership_pass

#       - name: Checkstyle
#         run: mvn checkstyle:check -B

#       - name: SpotBugs analysis
#         run: mvn spotbugs:check -B

#       - name: Package application
#         run: mvn package -DskipTests -B

#       - name: Upload JAR artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: membership-service-jar
#           path: target/membership-service-*.jar
#           retention-days: 7

#       - name: Upload test reports
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: test-reports
#           path: target/surefire-reports/
#           retention-days: 7
